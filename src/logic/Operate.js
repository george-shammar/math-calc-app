import Big from 'big.js';

const operate = (numberOne, numberTwo, operation) => {
    if (operation === '+') {
        const result = numberOne + numberTwo;
        return result;
    } else if (operation === '-') {
        const result = numberOne - numberTwo;
        return result;
    } else if (operation === 'X') {

    }
}